# 🚀 Быстрый старт

## Распаковка проекта

```bash
# Распакуйте архив
tar -xzf confluence_semantic_search.tar.gz

# Перейдите в директорию проекта
cd confluence_semantic_search
```

## Установка и запуск

### Шаг 1: Создание виртуального окружения

```bash
python3.12 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Настройка конфигурации

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
nano .env  # или используйте любой другой редактор
```

**Минимальная конфигурация:**
```env
CONFLUENCE_BASE_URL=https://your-confluence-domain.com
```

### Шаг 4: Запуск

```bash
python main.py
```

## Результат

Результаты будут сохранены в директории `output/` в формате JSON с временной меткой:
```
output/confluence_chunks_20240208_103000.json
```

## Что дальше?

1. **Изучите документацию:**
   - `README.md` - полная документация проекта
   - `EXAMPLES.md` - примеры использования и сценарии
   
2. **Настройте под себя:**
   - Размер чанков (`CHUNK_SIZE`)
   - Перекрытие (`CHUNK_OVERLAP`)
   - Стратегию чанкования (`CHUNKING_STRATEGY`)
   
3. **Интегрируйте с поиском:**
   - Загрузите чанки в векторную БД
   - Создайте эмбеддинги с помощью bge-m3
   - Реализуйте семантический поиск

## Поддержка

При возникновении проблем:
- Проверьте настройки в `.env`
- Включите `SHOW_PERFORMANCE_METRICS=true` для диагностики
- Убедитесь что Confluence доступен и URL корректный
