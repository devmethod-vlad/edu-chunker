# ─── Core dependencies ───
python-dotenv>=1.0,<2       # Загрузка переменных окружения из .env
httpx>=0.26,<1              # Современный async HTTP-клиент (connection pooling, retry)
beautifulsoup4>=4.12,<5     # Парсинг HTML
lxml>=5.0,<6                # Быстрый HTML-парсер для BeautifulSoup

# ─── Progress bar ───
tqdm>=4.66,<5               # Интерактивный прогресс-бар (опционально, нужен при SHOW_PROGRESS_BAR=true)

# ─── Fast JSON (опционально, ускоряет сериализацию) ───
orjson>=3.9,<4              # Быстрый JSON; если не установлен, используется стандартный json

# ─── Tokenizer strategy (опционально) ───
# Раскомментируйте, если используете CHUNKING_STRATEGY=tokenizer
# transformers>=4.36,<5
# sentencepiece>=0.1.99,<1

# ─── Sentence segmentation (опционально, для более умного split по предложениям) ───
# Раскомментируйте и выставьте SENTENCE_SPLITTER=razdel или SENTENCE_SPLITTER=spacy
razdel>=0.5.0
spacy>=3.7,<4
